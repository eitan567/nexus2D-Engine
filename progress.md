Original prompt: אני רוצה שתעבור על הפרוייקט שלי ותשפר אותו משמעותית מכול בחינה אפשרית גם UI וגם לוגיקה וכמובן הוספת כל מה שצריך מנוע 2D למשחקים. אני רוצה שתוסיף חיבור לבינה מלאכותית של GOOGLE באמצאות VERTEX AI - פרטי החיבור נמצאים בקובץ .env.local. המשתמש יוכל לבקש מהבינה ליצור עבורו משחקים במנוע או להוסיף לו אלמנטים חדשים למשחק קיים שנמצא בשלב פיתוח של המשתמש.

2026-03-14
- מיפוי ראשוני הושלם: הפרויקט הוא עורך React + Phaser עם רוב הלוגיקה מרוכזת ב-App.tsx ומנוע בסיסי ב-Core.ts.
- זוהו פערים מרכזיים: אין שכבת שרת בטוחה ל-AI, אין render_game_to_text או advanceTime, אין מודל עשיר למנוע 2D, ואין history/undo/export/import/scene tooling מסודרים.
- .env.local כולל project/location עבור Vertex AI, אך לא כולל credential file; יש לבנות אינטגרציה שמסתמכת על ADC/שרת ריצה ולא חושפת סודות לקליינט.
- תכנון נוכחי: להוסיף מודל נתונים עשיר יותר, utilities/editor state, API שרתית ל-Vertex AI, ופאנל AI עם יצירת משחק חדש/הרחבת משחק קיים.
- הושלמו שכבות בסיס חדשות:
  - `src/types.ts` הורחב לסכמת מנוע עשירה יותר עם `Behavior`, הגדרות scene ו-runtime snapshot.
  - `src/lib/project-utils.ts` מוסיף יצירת פרויקט/scene/prefabs, שכפול, נרמול JSON ו-context ל-AI.
  - `server/vertex-ai-middleware.ts` ו-`vite.config.ts` מחברים Vertex AI דרך middleware צד-שרת.
  - `src/engine/Core.ts` שוכתב עם behaviors, runtime state, `render_game_to_text`, `advanceTime`, fullscreen וטעינת assets.
  - `src/App.tsx`, `src/index.css`, `index.html`, `README.md` משוכתבים לכיוון של editor אמיתי עם AI assistant פנימי למנוע.
- הבהרת משתמש שולבה: ה-AI צריך להיות "מפתח חכם" בתוך המנוע עצמו, עם היכרות לסכמה, behaviors ויכולת לכתוב Script components בעת הצורך.
- בדיקות/Build/Playwright לא הורצו לפי בקשת המשתמש. בסיום יש לספק לו הוראות ידניות להרצה ואימות.
- תוקנו תקלות אתחול לוקאליות של Phaser:
  - renderer עבר לזיהוי מפורש במקום `AUTO` עם canvas חיצוני.
  - בוטל `StrictMode` ב-React כדי למנוע mount/unmount כפול של מנוע Phaser ב-dev.
  - readiness של scene הוקשח כדי לא לגעת ב-camera/input/physics לפני שהם זמינים.
  - keyboard binding עבר ל-`addKey(..., false)` כדי למנוע crash ב-`addCapture` בזמן boot.
- בוצע סבב UI נוסף בכיוון של editor מקצועי כהה:
  - הוחלפה פלטת הכחולים/הכפתורים הגדולים ב-chrome אפור צפוף עם accent חלש בלבד.
  - `src/App.tsx` קיבל חיפוש ל-`Outliner` ול-`Details`, בחירות active אפורות, ו-card chrome קשיח יותר.
  - `Content Browser` הועבר מה-sidebar ל-`Content Drawer` תחתון בסגנון מנוע מקצועי כדי לשחרר שטח ל-viewport.
  - `src/index.css` עודכן ל-layout קטן יותר, רדיוסים חדים יותר, status bar תחתון ו-drawer assets.
- לא הורצו build/lint/tests גם בסבב הזה, בהתאם לבקשת המשתמש. בבדיקה ידנית צריך לרענן את dev server/page ולוודא:
  - שאין גלילה של כל העמוד ב-desktop.
  - שה-viewport נשאר האלמנט המרכזי וה-sidebars גולשים פנימית בלבד.
  - ש-`Content Drawer` נפתח/נסגר ומציג assets מיובאים.
  - שחיפוש ב-`Outliner` וב-`Details` מסנן כמו שצריך.
- בוצע סבב נוסף לפי פידבק המשתמש על workflow מקצועי יותר:
  - `createDefaultProject()` עבר ל-blank project במקום demo project, עם scene ריק וברירת מחדל אפורה.
  - נוסף `Project Launcher` מלא שמופיע בכניסה למנוע ומאפשר:
    - פרויקט ריק חדש.
    - Resume ל-session האחרון מה-local storage.
    - טעינת sample project של platformer או top-down.
    - import של פרויקט JSON קיים.
  - ה-editor קיבל `World` / `Camera` stage modes:
    - `World` מציג את כל ה-world ומשרטט camera frame של מה שהשחקן יראה בפועל.
    - `Camera` מציג preview קרוב יותר לתצוגת המשחק עצמה.
  - overlays של metrics הוצאו מאזור ה-canvas ל-bars של chrome, כדי שה-viewport יישאר נקי יותר.
  - grid ורקע ב-`Core.ts` הוחלפו למראה אפור יותר עם major/minor lines ו-world bounds גלויים.
  - `nexus-panel-body` קיבל `height: 0` ו-`flex: 1 1 0%` כדי לכפות גלילה פנימית בפאנלים.
- עדיין לא הורצו build/lint/tests. בבדיקה ידנית הבאה צריך לוודא:
  - שבכניסה רואים launcher ולא editor פתוח על demo scene.
  - ש-`New Blank Project` פותח scene ריק בלי entities מוכנים מראש.
  - ש-`World` mode מציג את כל המפה וה-frame של המצלמה.
  - ש-`Camera` mode חוזר ל-preview הדומה יותר למשחק.
  - שה-click/drag/select על entities עובדים גם אחרי המעבר ל-world-fit camera.
- בהתאם לפידבק נוסף של המשתמש:
  - הוסר מה-chrome הקבוע של המנוע HUD משחקי קשיח כמו `Score`, `Deaths` ו-`Collectibles`.
  - שורת ה-status וה-stage meta מציגות עכשיו מידע engine-level בלבד: camera, zoom, selection, simulation state, snap/follow/world settings.
- סבב editor interaction נוסף:
  - נוסף `cameraStart` ל-`SceneSettings`, עם עריכה מתוך ה-inspector וכפתור `Use Current View` כדי לקבע את תחילת המצלמה מתוך ה-viewport.
  - נוספה תנועת `pan` ל-editor עם `Middle Mouse`, בנוסף ל-`Ctrl/Cmd + Wheel` עבור zoom.
  - כפתור ה-`Delete` הראשי עבר לפעול לפי context: actor נבחר ימחק actor, ואם אין selection הוא ימחק או יאפס את ה-scene הפעיל.
  - נוספה מחיקת scene מפורשת גם מתוך רשימת ה-`Levels` וגם מתוך תפריט `Scene`.
- תוקן לופ של `Maximum update depth exceeded` ב-`App.tsx` לאחר כניסה ל-editor:
  - `fitStageCanvas` עודכן לעדכן `stageCanvasSize` רק אם width/height באמת השתנו (idempotent state update).
  - הוסרו dependencies של `useEffectEvent` מ-effects (`resize`, `hotkeys`, ו-`fitStageCanvas` observer effect) כדי למנוע rerun על כל render.
- אימות ריצה:
  - `web_game_playwright_client` רץ עם `--click-selector "text=New Blank Project"` (iterations=1) ללא `errors-0.json` חדש.
  - בדיקת Playwright ממוקדת עם המתנה של 4 שניות אחרי כניסה ל-editor החזירה `MAX_DEPTH_ERRORS 0`.
- `npm run lint` עדיין נכשל עקב שגיאות TypeScript קיימות מראש שאינן חלק מהתיקון הנוכחי.
- אימות נוסף: הרצה עם `--click-selector "text=Load Platformer Sample"` (iterations=1) ללא יצירת `errors-0.json` חדש.
- תוקן jitter בכניסה לעורך שבו ה-grid/canvas נמתח מאוחר:
  - מדידת stage הועברה ל-`useLayoutEffect` כדי לחשב גודל לפני paint.
  - הוסר `layout` מ-`motion.div` של ה-canvas frame כדי לבטל layout-animation בזמן אתחול.
- אימות:
  - `web_game_playwright_client` עם `New Blank Project` עבר ללא `errors-0.json` חדש.
  - דגימת `render_game_to_text` כל ~120ms הראתה camera size יציב (`706x406`) כבר מהדגימה הראשונה ועד ~1s, ללא שינוי מאוחר.
- עודכן קלט מצלמה לפי בקשת המשתמש (פאנינג וזום חופשיים):
  - `App.tsx`: Pan מתחיל עם `Middle` או `Right` mouse (גם בזמן סימולציה), ונוסף `onContextMenu` לקנבס כדי למנוע תפריט ימני בזמן drag.
  - `App.tsx`: Zoom עם גלגלת על ה-grid עובד תמיד (ללא `Ctrl`), כל עוד ה-launcher לא פתוח.
  - שורת סטטוס עודכנה כדי לשקף את הקיצורים החדשים.
  - `Core.ts`: הוסרה חסימת `isPlaying` מ-`panEditor` ו-`adjustEditorZoom`.
  - `Core.ts`: נוספה `manualCameraControlActive` כדי לאפשר pan/zoom ידני גם כשה-simulation רץ עם `cameraFollowPlayer`; override מתאפס ב-`setRunning`.
- אימות:
  - `web_game_playwright_client` עם `New Blank Project` עבר ללא `errors-0.json` חדש.
  - בדיקת Playwright ממוקדת על `Load Platformer Sample` הראתה:
    - `MAX_DEPTH = 0`.
    - בזמן runtime zoom השתנה מ-`1` ל-`0.53` עם wheel ללא Ctrl.
    - לאחר pan ב-right-drag, `camera.x` השתנה (למשל `164.9 -> 469.2`).
- כיוונון נוסף לגבולות pan:
  - ב-`clampCameraToScene` הוגדל/הודק `editorPanPadding` כך שבקצוות אפשר לראות באופן עקבי שטח אפור מחוץ למסגרת ה-grid.
  - padding מחושב דינמית גם לפי גודל viewport (כ-24%) וגם לפי grid size כדי לשמור התנהגות עקבית בזומים שונים.
- אימות ממוקד (Playwright, Platformer runtime):
  - אחרי zoom-out + pan, מתקבל מצב שבו `camera.x + camera.width > worldWidth` ו/או `camera.y + camera.height > worldHeight` (כלומר רואים מחוץ לגריד).
  - לא הופיעה שגיאת `Maximum update depth exceeded` חדשה.
- תוקנה יציבות גרירת Actors (קפיצות בזמן Move):
  - `App.tsx`: גרירת Transform עברה לעיבוד עם `requestAnimationFrame` (במקום עדכון על כל mousemove) כדי למנוע backlog/jitter.
  - `App.tsx`: בזמן drag נקודת העכבר נ-clamp לגבולות הקנבס לצמצום קפיצות כשעוזבים את אזור הציור.
  - `App.tsx`: נוספה מניעת no-op updates (לא נשלח שינוי היסטוריה אם Transform בפועל לא השתנה).
  - `App.tsx`: בזמן drag, zoom-wheel מושבת זמנית כדי למנוע קפיצות שנגרמות מאירועי גלגלת תוך כדי הזזה.
  - `App.tsx`: ניקוי `drag` pending frame מתבצע גם ב-`mouseup`/`blur` גלובלי.
- אימות:
  - `web_game_playwright_client` (New Blank Project) עבר ללא `errors-0.json` חדש.
  - בדיקת Playwright ממוקדת (כניסה לעורך, הוספת Player, drag + wheel תוך drag) החזירה `maxDepth: 0`, `pageErrors: 0`.
- נוספה יכולת `Auto Size` ל-Collider:
  - `types.ts`: הוסף `collider.autoSize`.
  - `project-utils.ts`: ברירת מחדל `autoSize: false` + normalization לפרויקטים ישנים.
  - `App.tsx` (Inspector): נוסף Toggle `Auto Size` תחת Collider; כשפעיל, שדות Radius/Width/Height מוצגים לפי גודל Sprite+Scale ומנוטרלים לעריכה.
  - `Core.ts`: physics collider וה-overlay משתמשים במדדים מחושבים בזמן אמת לפי `sprite.displayWidth/displayHeight` כשהדגל פעיל.
- אימות:
  - בדיקת Playwright: הוספת Player, הפעלת `Auto Size`, שינוי רוחב Sprite; ב-localStorage נשמר `colliderAutoSize: true` ו-`spriteWidth` החדש.
  - לא הופיעה שגיאת `Maximum update depth exceeded` חדשה.
- שופר `Collider Auto Size` כך שיתחשב גם ב-`Rotation`:
  - `App.tsx`: חישוב הערכים המוצגים ב-Inspector (`Width/Height/Radius`) עבור Auto Size מבוסס עכשיו על AABB של sprite מסובב.
  - `Core.ts`: `resolveColliderMetrics` עודכן לאותו חישוב (AABB לפי זווית), כך שגם גוף הפיזיקה בפועל וגם overlay collider מותאמים לסיבוב.
- אימות ממוקד:
  - ב-`Load Platformer Sample`, עבור Player עם `Auto Size` פעיל:
    - לפני סיבוב: `Width=72`, `Height=96`.
    - אחרי `Rotation=45`: `Width≈118.79`, `Height≈118.79`.
  - כלומר Auto Size מגיב לסיבוב כפי שנתבקש.

2026-03-15
- יושם Accordion לכל `SectionCard` בפאנלים:
  - `App.tsx`: לכל כותרת section נוסף חץ קטן (`ChevronRight`) וכפתור toggle.
  - גוף ה-section נסגר/נפתח בלחיצה על הכותרת, עם `aria-expanded` לטובת נגישות.
  - נשמרה תמיכה ב-`action` הקיים (למשל `Remove`) בלי לשנות behavior.
- `index.css`:
  - נוספו מחלקות `nexus-section-toggle` ו-`nexus-section-chevron` כולל סיבוב חץ במצב פתוח (`is-open`) ו-hover ברור.
- אימות:
  - `web_game_playwright_client` רץ מול `New Blank Project` ללא `errors-0.json` חדש.
  - בדיקת Playwright ממוקדת (DOM) אישרה toggle תקין:
    - לפני: `before=1` (תוכן מוצג)
    - אחרי סגירה: `afterCollapse=0`
    - אחרי פתיחה: `afterExpand=1`
    - `aria-expanded`: `false` בסגירה ו-`true` בפתיחה.
- שופר zoom אוטומטי במצב `World` כדי למנוע כיווץ אנכי לא רצוי בזמן הגדלת world:
  - `Core.ts`: נוסף `worldFillReferenceZoom` ששומר את baseline הויזואלי ל-world view.
  - כשה-world גדל, הזום הבסיסי לא יורד אוטומטית ולכן ה-grid לא "מתכווץ" לגובה בכל שינוי רוחב.
  - reset ל-baseline מתבצע בהחלפת מצב viewport (`world/camera`), מעבר scene ושינוי גודל חלון.
- אימות:
  - `web_game_playwright_client` עבר ללא `errors-0.json`.
  - בדיקת Playwright ממוקדת:
    - `World Width=1200` ואז `2200` תוך `World Height=1350`.
    - בתמונות לפני/אחרי נשמר קנה מידה אנכי יציב (ללא shrink אנכי חד), והמצלמה נשארת יציבה סביב `Current view: 0,0`.
- תיקון נוסף לבאג zoom-lock בזמן מחיקת `World Width` והקלדה מחדש:
  - שורש התקלה: ערכי ביניים קטנים בזמן הקלדה (למשל `3`, `30`, `300`) הקפיצו את `worldFillReferenceZoom` למעלה, ואז zoom-out הרגיש "תקוע".
  - `Core.ts`: `getWorldBaseZoom` שומר baseline יציב ולא מעלה אותו בזמן עריכה דינמית של world size.
  - `Core.ts`: `getWorldFitZoom`/`getWorldFillZoom` קיבלו הגנה עם `Math.max(1, worldSize)` למניעת חלוקה ב-0.
  - `App.tsx` (Scene Settings): `World Width/Height` עוברים clamp למינימום `1` בזמן עדכון state.
- אימות תיקון:
  - `web_game_playwright_client` עבר ללא `errors-0.json`.
  - בדיקת Playwright ממוקדת הדמיה של רצף הקלדה (`3 -> 30 -> 300 -> 3000`) הראתה ש-zoom-out עובד אחרי העריכה (לפני/אחרי wheel נראית התכווצות grid צפויה, ללא lock).
- שיפור UX פוקוס ב־Viewport המרכזי:
  - `App.tsx`: נוסף `focusStageViewport` שמבצע `blur` לשדות קלט פעילים (`input/textarea/select/contentEditable`) ומעביר פוקוס ל־`canvas`.
  - `App.tsx`: נוסף `onPointerDownCapture={focusStageViewport}` על `nexus-stage-shell` כך שכל לחיצה בתצוגה המרכזית תעביר פוקוס לבמה.
  - `App.tsx`: הקנבס קיבל `tabIndex={0}` כדי שיוכל לקבל פוקוס בפועל.
- אימות:
  - `web_game_playwright_client` עבר ללא `errors-0.json`.
  - בדיקת Playwright ממוקדת: לפני לחיצה בתצוגה `activeElement` היה `INPUT type=number`, ואחרי לחיצה בתצוגה `activeElement` עבר ל-`CANVAS`.
- נוספה יכולת להזיז את מסגרת המצלמה עם `Ctrl/Cmd + Drag` בתוך שטח המצלמה:
  - `App.tsx`: `PanState` הורחב למוד נפרד `camera-drag` בנוסף ל-`editor-pan`.
  - `App.tsx`: ב-`handleCanvasMouseDown`, לחיצה שמאלית עם `Ctrl/Cmd` בתוך rect של המצלמה (ב-`World` mode) מתחילה גרירת מצלמה.
  - `App.tsx`: ב-`handleCanvasPointerMove`, בזמן `camera-drag` מתעדכן `scene.settings.cameraStart` בזמן אמת עם clamp לגבולות ה-world.
  - `App.tsx`: שורת הסטטוס עודכנה לקיצור החדש (`Ctrl/Cmd+Drag inside camera frame...`).
- אימות:
  - `web_game_playwright_client` עבר ללא `errors-0.json`.
  - בדיקת Playwright ממוקדת:
    - `Ctrl+Drag` בתוך מסגרת המצלמה העביר `Camera Start` מ-`0,0` ל-`481,263`.
    - `Ctrl+Drag` מחוץ למסגרת המצלמה השאיר `Camera Start` ללא שינוי (`0,0`).
- שיפור יציבות גרירת מצלמה (`Ctrl/Cmd + Drag`) כדי למנוע קפיצות:
  - `App.tsx`: ב-`camera-drag` החישוב עבר מדלתא world-point לדלתא screen/zoom יציבה (`dx,dy = (screenDelta / zoomStart)`).
  - `App.tsx`: בתחילת `camera-drag` מתבצע `engineRef.current?.panEditor(0, 0)` כדי לקבע `editorCameraCenter` ולמנוע שינוי פריים בזמן drag.
  - `PanState` עודכן בהתאם: הוסר `worldStart`, נוסף `zoomStart`.
- אימות:
  - `web_game_playwright_client` עבר ללא `errors-0.json`.
  - בדיקת Playwright ממוקדת (גרירת מצלמה עם `Ctrl`) עדיין מזיזה תקין את `Camera Start` מ-`0,0` ל-`481,263` עם תנועה ויזואלית יציבה יותר.
- שופר spawn של Actors כדי שייווצרו ביחס למיקום הנוכחי אבל תמיד בתוך אזור המצלמה:
  - `App.tsx` (`addPrefab`): מחושב `cameraFrame` מה-`cameraStart` הנוכחי + גודל viewport.
  - anchor ליצירה מבוסס על entity נבחר (עם offset יחסי), או מרכז ה-`cameraFrame` אם אין selection.
  - אחרי יצירת prefab, ה-`Transform` של הישות החדשה עובר clamp לגבולות `cameraFrame` לפי גודל sprite/scale.
- Collider ברירת מחדל:
  - `project-utils.ts`: `createCollider()` עבר ל-`autoSize: true` כברירת מחדל.
  - `project-utils.ts` normalization ל-Collider עודכן כך שברירת המחדל ל-`autoSize` היא `true` כשאין ערך מפורש.
- אימות:
  - `web_game_playwright_client` עבר ללא `errors-0.json`.
  - בדיקת Playwright ממוקדת:
    - אחרי `Camera Start = 700,240` והוספת `Player`, הישות הופיעה בתוך `cameraFrame` (`insideCamera: true`).
    - ב-state שנשמר, ל-`Collider` של השחקן החדש `autoSize: true`.
- תיקון בטיחות למחיקת Selection עם מקלדת:
  - `App.tsx` (`handleHotkeys`): `Delete/Backspace` מוחקים רק `selectedEntity` קיים.
  - הוסרה מחיקת scene מה-hotkey כשאין selection (מניעת מחיקה גורפת בלחיצה מתוך ה-viewport).
  - נוסף `event.preventDefault()` ל-`Delete/Backspace` כדי למנוע התנהגות ברירת-מחדל לא רצויה.
- אימות:
  - `web_game_playwright_client` (New Blank Project) עבר ללא `errors-0.json`.
  - בדיקת Playwright ממוקדת על `Load Platformer Sample`:
    - ללא selection: כפתור מסוכן נשאר `Reset Scene`, וספירת entities נשארה `9 -> 9` אחרי `Delete`.
    - עם selection: ספירת entities ירדה `9 -> 8` אחרי `Delete` (מחיקת actor בלבד).
- הקשחה נוספת ל־`Delete/Backspace` בעקבות דיווח משתמש:
  - `App.tsx` (`handleHotkeys`): נוספו `event.stopPropagation()` ו־`event.stopImmediatePropagation?.()` כדי לחסום מאזינים אחרים.
  - מאזין `window.keydown` עבור hotkeys הועבר ל־capture phase (`addEventListener(..., true)`).
- אימות נוסף:
  - `web_game_playwright_client` (Load Platformer Sample) עבר ללא `errors-0.json`.
  - בדיקה ממוקדת לפני/אחרי `Delete` ללא selection: `entities 9 -> 9`, `scenes 1 -> 1`, label נשאר `Reset Scene`.
  - השוואת screenshot מלאה לפני/אחרי (`delete-debug-before.png` / `delete-debug-after.png`) לא הראתה שינוי ויזואלי קבוע במסגרת ה-grid בריצה האוטומטית.
- זוהתה סיבת השורש לשינוי הוויזואלי אחרי `Delete` ב־viewport:
  - הבעיה לא הייתה מחיקת אובייקט/scene אלא `outline` דפדפן של ה־`canvas`.
  - לפני `Delete` ה־canvas היה בפוקוס עם `outline: none 0px`, ואחרי `Delete` Chromium שינה אותו ל־`auto 1px`, מה שנראה כמו מסגרת כהה סביב ה־grid.
  - `index.css`: נוספו `canvas { outline: none; display: block; }` ו־`.nexus-canvas-frame:focus-within` עם highlight יציב במקום outline דפדפן.
- אימות:
  - בדיקת Playwright ממוקדת ב־`New Blank Project`:
    - לפני `Delete`: `activeTag=CANVAS`, `canvasOutline=none`, `frameBoxShadow` פעיל.
    - אחרי `Delete`: `activeTag=CANVAS`, `canvasOutline=none`, `frameBoxShadow` נשאר זהה.
  - `web_game_playwright_client` (New Blank Project) עבר ללא `errors-0.json`.
- שודרג מנגנון ה־transform של entities כך ש־rotate/scale יתבצעו דרך ידיות ייעודיות:
  - `App.tsx`: `DragState` הורחב ל־`interaction` נפרד (`move` / `rotate` / `scale`) עם מידע על handle וכיוון scale.
  - `App.tsx`: נוסף gizmo HTML/SVG מעל ה־canvas עם 4 ידיות scale וידית rotate עליונה, כולל `data-transform-handle` לבדיקות.
  - `App.tsx`: ב־`rotate/scale` mode גרירה מתוך גוף האובייקט כבר לא מבצעת transform; הפעולה מתחילה רק מהידיות.
  - `App.tsx`: hit-test של entity עודכן להיות לפי rect מסובב במקום AABB פשוט.
  - `Core.ts`: הוסר ה־overlay הישן של rotate/scale מתוך Phaser כדי למנוע כפילות ויזואלית.
  - `Core.ts`: נוסף `worldToScreen()` כדי לחשב את מיקום הידיות לפי המצלמה האמיתית של editor, לא לפי preview camera.
  - `index.css`: נוספו סגנונות gizmo וידיות, עם highlight ברור למצב transform פעיל.
- אימות:
  - בדיקת Playwright ממוקדת על `New Blank Project` + `Player`:
    - drag מגוף האובייקט בזמן `Rotate` לא שינה `rotation`.
    - drag מידית rotate שינה `rotation` מ־`0` ל־`104.7`.
    - drag מגוף האובייקט בזמן `Scale` לא שינה `scale`.
    - drag מידית scale שינה `scale` ל־`x≈3.15`, `y=0.2`.
    - drag במצב `Move` עדיין שינה `position` מ־`577,329.5` ל־`823.25,473.34`.
  - נבדקו צילומי מסך מלאים:
    - `transform-gizmo-before.png` מציג מסגרת selection + 4 ידיות scale + ידית rotate.
    - `transform-gizmo-after.png` מציג את האובייקט אחרי rotate/scale/move עם הגיזמו מותאם.
  - `web_game_playwright_client` רץ שוב ללא `errors-0.json` חדש.
- מצב lint:
  - `npm run lint` עדיין נכשל על שגיאות TypeScript קיימות מראש מחוץ לפיצ'ר הזה, בנוסף לשגיאות ישנות ב־`App.tsx` / `project-utils.ts`.
- שופר workflow ההזזה של actors:
  - `App.tsx`: גרירת גוף האובייקט מפעילה תמיד `move` interaction, גם אם toolbar מסומן כרגע על `Rotate` או `Scale`.
  - `App.tsx`: הוסרה התלות במצב `Move` כדי להתחיל drag מתוך שטח הישות.
  - `App.tsx` / `index.css`: סמן ברירת המחדל של ה־canvas שונה מ־`crosshair` ל־`pointer`.
  - `App.tsx`: שורת הסטטוס עודכנה כדי להבהיר שאפשר לגרור actors ישירות מהגוף שלהם.
- אימות:
  - בדיקת Playwright ממוקדת על `New Blank Project` + `Player`:
    - במצב `Rotate`, drag מתוך גוף האובייקט הזיז את ה־actor (`position` השתנה) בלי לשנות `rotation`.
    - במצב `Scale`, drag מתוך גוף האובייקט הזיז את ה־actor בלי לשנות `scale`.
    - ידית rotate עדיין עובדת ומשנה `rotation` אחרי ההזזה.
    - `getComputedStyle(canvas).cursor === 'pointer'`.
  - `web_game_playwright_client` רץ שוב ללא `errors-0.json` חדש.
- תוקן קונפליקט `Space` עם סימולציה:
  - `App.tsx`: הוסר hotkey של `Space` שהפעיל/עצר את הסימולציה מתוך `handleHotkeys`.
  - `App.tsx`: נוסף `toggleSimulation()` שמעביר פוקוס ל־canvas מיד אחרי לחיצה על כפתור `Simulate/Stop`.
  - `App.tsx`: כפתור `Simulate/Stop` קיבל `onMouseDown={preventDefault}` כדי לא להישאר עם focus ככפתור לחיץ ב־Space.
- סיבת שורש:
  - גם אחרי הסרת hotkey, כפתור `Simulate` עצמו נשאר בפוקוס אחרי click, ולכן `Space` הפעיל את הכפתור שוב ברמת הדפדפן והחזיר את המנוע ל־`editor`.
- אימות:
  - בדיקת Playwright ממוקדת על `Load Platformer Sample`:
    - אחרי click על `Simulate`, `document.activeElement` עבר ל־`CANVAS`.
    - לחיצה על `Space` השאירה את `mode` על `play` לאורך כל הבדיקה (`before/during/after`).
  - `web_game_playwright_client` רץ שוב ללא `errors-0.json` חדש.
- תוקנה נפילת `Player` ב־`Simulate` כשהסצנה מכילה רק שחקן:
  - `Core.ts`: הועבר `solidGroup.add(sprite)` לפני קונפיגורציית physics של ה־body.
  - סיבת שורש: חיבור ה־sprite לקבוצת הקוליזיות אחרי הגדרת ה־body דרס חלק מהגדרות הגוף, כך ש־gravity לא נכנסה ל־`Player` עד ש־`ArrowDown` הוסיף מהירות Y ידנית.
- אימות:
  - בדיקת Playwright ממוקדת על `New Blank Project` + `Player`:
    - מיד אחרי `Simulate`, ה־snapshot כבר הראה `vy > 0`.
    - אחרי ~700ms ה־`Player` ירד מ־`y≈354.72` ל־`y≈543.37` בלי שום קלט נוסף.
    - בבדיקה דטרמיניסטית עם `advanceTime()` ה־`Player` נפל, עשה respawn כשעבר את תחתית העולם, ו־`deaths` עלה בהתאם.
- תוקן respawn של `Player` אחרי נפילה מתחת לעולם:
  - `Core.ts`: `handlePlayerRespawn()` עבר מ־`sprite.setPosition() + body.setVelocity(0,0) + body.updateFromGameObject()` ל־`body.reset(spawnX, spawnY)`.
  - סיבת שורש: `updateFromGameObject()` לא מיועד ל־resync מלא של Dynamic Body. זה השאיר את ה־body במצב לא תקין אחרי teleport, וגרם ל־`y` לקפוץ לערכים שליליים עצומים אחרי respawn.
- אימות:
  - בדיקה דטרמיניסטית סביב רגע המוות:
    - לפני התיקון, אחרי `deaths = 1` ה־`Player` קפץ ל־`y≈-467`, `-1286`, `-2104`... למרות שצריך היה לחזור ל־spawn.
    - אחרי התיקון, בפריים הראשון אחרי respawn ה־`Player` חזר ל־`y≈352.27` עם `vy≈16.33`, ואז האיץ מטה בהדרגה (`32.67`, `49`, `65.33`...).
  - נבדק גם ויזואלית בצילום `respawn-check.png`.
- נוסף `Ctrl/Cmd+Shift+Drag` בתוך מסגרת המצלמה ב־`World` view:
  - `App.tsx`: `Ctrl/Cmd+Drag` נשאר להזזת `cameraStart` בלבד.
  - `App.tsx`: `Ctrl/Cmd+Shift+Drag` מזיז את `cameraStart` ואת כל ה־entities שהיו בתוך פריים המצלמה בתחילת הגרירה, באותו delta.
  - `App.tsx`: לכידת entities נעשית לפי חיתוך bounds מול פריים המצלמה, לא רק לפי center point.
  - תוקן גם bug מקומי: שימוש ב־`new Map(...)` בתוך `App.tsx` דרס את אייקון `Map` מ־`lucide-react`; הוחלף ל־`new globalThis.Map(...)`.
- אימות:
  - בדיקת Playwright ממוקדת:
    - `Ctrl+Drag` הזיז רק את המצלמה (`camera 0,0 -> 346,231`) בלי לשנות את מיקומי ה־entities.
    - `Ctrl+Shift+Drag` הזיז גם את המצלמה וגם את ה־entities באותו delta:
      - `player 513,352 -> 859,583`
      - `platform 577,320 -> 923,551`
      - `camera 0,0 -> 346,231`
- נוספה בחירת marquee מרובה על ה־grid:
  - `App.tsx`: נוסף `selectedEntityIds` בנוסף ל־`selectedEntityId` הראשי.
  - `App.tsx`: drag על שטח ריק ב־viewport יוצר box selection ובוחר את כל ה־actors שחוצים את האזור.
  - `App.tsx`: `Delete` ו־`Duplicate` תומכים עכשיו גם ב־multi-selection.
  - `App.tsx` / `index.css`: נוספו overlay של תיבת הבחירה והיילייט לכל actors שנבחרו.
  - `App.tsx`: ב־Details מוצג summary של multi-selection במקום inspector של actor יחיד.
- אימות:
  - בדיקת Playwright ממוקדת:
    - `New Blank Project` + `Player` + `Platform` + `Enemy`.
    - drag marquee על האזור החזיר `3 actors selected`.
    - לחיצה על `Delete` אחרי ה־multi-selection מחקה את שלושתם (`entities -> []`).
    - `Duplicate` על multi-selection של `Player` + `Platform` יצר `Player Copy` ו־`Platform Copy` (`entityCount 2 -> 4`).
  - נבדק גם ש־`addPrefab` עדיין מסמן את ה־actor החדש מיד, וש־`selectedEntityId` ממשיך להופיע ב־runtime snapshot.
- תוקן layout הסרגל התחתון (`footer`):
  - `App.tsx`: `RuntimeBadge` קיבל מחלקות ייעודיות + `title` לערכים ארוכים.
  - `App.tsx`: טקסט הסטטוס של ה־editor רוכז ל־`footerStatusText` וקוצר מעט כדי שייכנס באופן קריא יותר.
  - `index.css`: `footer` עבר ל־grid של שני אזורים קבועים (`meta` + `status`), כדי שה־badges לא יתכווצו וישברו לשתי שורות.
  - `index.css`: badges בתחתית הוגדרו כ־`nowrap` עם ellipsis לערכים ארוכים, ו־`nexus-status-line` עבר ל־2 שורות קבועות עם line clamp.
- אימות:
  - `web_game_playwright_client` רץ על `Load Platformer Sample` ללא `errors-0.json` חדש.
  - בדיקת Playwright מלאה של ה־DOM + screenshot (`footer-full.png`) הראתה:
    - `Scene`, `Selection`, `AI` נשארים בגובה שורה אחת (`wrapped: false` לכל badge).
    - טקסט הסטטוס נסגר ל־2 שורות (`-webkit-line-clamp: 2`, `statusHeight ≈ 29.7px`).
- סמן ה־viewport הוחזר לחץ רגיל:
  - `index.css`: `nexus-canvas-frame` חזר מ־`cursor: pointer` ל־`cursor: default`.
  - `App.tsx`: מחלקת ה־canvas חזרה מ־`cursor-pointer` ל־`cursor-default` בזמן idle; `cursor-grabbing` נשאר רק בזמן pan/drag פעיל.
- אימות:
  - `web_game_playwright_client` רץ על `Load Platformer Sample` ללא `errors-0.json` חדש.
  - בדיקת Playwright ממוקדת אישרה `frameCursor === 'default'` ו־`canvasCursor === 'default'`.
  - screenshot מלא (`cursor-page.png`) הראה שהעורך עולה תקין אחרי השינוי.
- מערכת ה־cursor ב־viewport עודכנה לפי המצבים בפועל:
  - `App.tsx`: נוסף `hoveredCanvasEntityId` כדי לזהות hover על גוף actor בתוך ה־grid.
  - `App.tsx`: ה־canvas frame מקבל עכשיו מחלקות מצב `is-hovering-entity` / `is-dragging` לפי hover ו־drag אמיתי, במקום cursor גורף קבוע.
  - `App.tsx`: ידית rotate קיבלה cursor ייעודי של סיבוב, וידיות scale שומרות על resize cursors הרגילים.
  - `index.css`: נוסף custom rotate cursor (`--cursor-rotate`), `grab` על hover של actor, ו־`grabbing` שמדרס גם את ה־handles בזמן כל drag/pan פעיל.
- אימות:
  - `web_game_playwright_client` רץ על `Load Platformer Sample` ללא `errors-0.json` חדש.
  - בדיקת Playwright ממוקדת החזירה:
    - `empty-grid`: `frameCursor = default`
    - `entity-hover`: `frameCursor = grab`
    - `scale-hover`: `scaleCursor = nwse-resize`
    - `rotate-hover`: `rotateCursor` הוא `url(...), grab`
    - `entity-drag`: `frameCursor = grabbing`, וגם `rotateCursor/scaleCursor = grabbing`
    - `Ctrl+Drag` בתוך מסגרת המצלמה: `frameCursor = grabbing`
  - screenshot מלא (`cursor-modes-page.png`) הראה שה־gizmo וה־viewport נשארו תקינים אחרי שינוי ה־cursors.
- תוקנה בעיית ה־Collider שבה `Player` היה שוקע בתוך `Platform` למרות `Auto Size`:
  - שורש התקלה היה ב־`Core.ts`: `resolveColliderMetrics()` מחזיר מידות world/display נכונות, אבל אותן מידות הוזנו ישירות ל־`Arcade Body.setSize()`.
  - ב־Phaser Dynamic Body, `setSize()` מצפה ל־source size ומכפיל שוב ב־scale של ה־sprite, לכן נוצרו גופים שגויים:
    - `Platform` בגובה קטן מדי
    - `Player` בגובה קטן מדי
  - `Core.ts`: נוסף `resolveBodyColliderMetrics()` שממיר world-space collider metrics ל־source-size/source-offset לפני `setSize` / `setOffset`.
  - `Core.ts`: אחרי החלת הגודל וה־offset מתבצע `body.updateFromGameObject()` כדי לסנכרן את הגוף מיידית.
  - `App.tsx`: `addPrefab()` עודכן כך שאם actor חדש היה נוצר בחפיפה עם actor נבחר, הוא מוזז אוטומטית לציר הקרוב ביותר שמפריד ביניהם. במקרה של `Platform` ואז `Player`, השחקן נוצר מעל הפלטפורמה במקום חצי בתוכה.
- אימות:
  - `web_game_playwright_client` רץ אחרי התיקון ללא `errors-0.json` חדש.
  - שחזור ממוקד `New Blank Project -> Platform -> Player -> Simulate`:
    - לפני התיקון: `Player` נוצר ב־`y=275` וחפף לפלטפורמה.
    - אחרי התיקון: `Player` נוצר ב־`y=243`, כלומר יושב בדיוק מעל הפלטפורמה.
    - בזמן `Simulate`, ה־runtime הראה `player.y = 243` ו־`vy = 0` לאורך ההמתנה, כלומר אין שקיעה ואין נפילה דרך הפלטפורמה.
  - בדיקת רגרסיה: `Player` לבד עדיין נופל רגיל (`y: 307 -> 541.38`, `vy: 686` אחרי ~800ms).
- תוקנה רגרסיית `Pass Through` בזמן `Simulate`:
  - שורש התקלה לא היה ב־`pass-through` עצמו אלא במסלול `engine.setProject() -> syncProject() -> syncEntity()` שנקרא על כל שינוי inspector גם בזמן ריצה.
  - `syncEntity()` דרס כל פעם את `sprite.setPosition(...)` ואת `body.setVelocity(...)` מערכי ה־project, ולכן שינוי `Pass Through` באמצע סימולציה החזיר את `Player` ל־spawn עם `vx/vy = 0` ויצר תחושה של restart.
  - `Core.ts` עודכן כך שבזמן `play`, עבור גופים דינמיים קיימים, נשמרים `x/y/angle/vx/vy` מה־runtime לפני סנכרון ונכתבים חזרה אחרי עדכון הגדרות ה־body וה־collider. כך שינויי inspector חיים עדיין מוחלים, אבל ה־runtime state לא מתאפס.
- אימות:
  - שחזור Playwright ממוקד אישר שלפני התיקון toggle של `Pass Through` בזמן ריצה החזיר את השחקן מ־`x=737.89, y=294.72, vx=240, vy=326.67` ל־`x=577, y=243, vx=0, vy=0`.
  - אחרי התיקון, אותו toggle השאיר את השחקן בתנועה רציפה (`x=731.89, y=284.65, vy=294` לפני, ואז `x=735.67, y=378.02, vy=522.67` אחרי), ללא reset של המצלמה או של ה־player state.
  - `web_game_playwright_client` רץ אחרי השינוי ללא שגיאת runtime חדשה.
  - `npm run lint` עדיין נכשל, אבל רק על שגיאות TypeScript ישנות ב־`App.tsx` ו־`project-utils.ts`, לא על הקוד החדש ב־`Core.ts`.
- UX: `Simulate` מעביר עכשיו גם את `STAGE VIEW` ל־`camera` ברמת ה־UI state, לא רק בהתנהגות של ה־engine.
  - `App.tsx`: `toggleSimulation()` עודכן כך שבמעבר `editor -> play` נקרא `setStageViewportMode('camera')` לפני flip של `isPlaying`.
- אימות:
  - בדיקת DOM ממוקדת הראתה מעבר `Stage View world -> Stage View camera` מיד אחרי לחיצה על `Simulate`.
  - screenshot ממוקד (`stage-view-simulate.png`) הראה שהכפתור העליון `CAMERA` מסומן ושגם ה־pill של `STAGE VIEW` מציג `camera` בזמן `play`.
  - `web_game_playwright_client` רץ אחרי השינוי ללא שגיאת runtime חדשה.
- UX: יציאה מ־`Simulate` מחזירה עכשיו את `STAGE VIEW` למצב שהיה לפני הכניסה ל־play.
  - `App.tsx`: נוספו `startSimulation()` ו־`stopSimulation()`.
  - בכניסה ל־play נשמר `stageViewportMode` המקורי ב־ref, ה־UI עובר ל־`camera`, וביציאה מוחזר המצב השמור.
  - הוחלפו קריאות ישירות ל־`setIsPlaying(false)` במסלולי יציאה מרכזיים (`launchProject`, החלפת scene, יצירת scene, החלת AI, מחיקת scene פעילה) כדי לא לעקוף את שחזור ה־stage view.
- אימות:
  - `world -> simulate -> stop` החזיר `Stage View world -> camera -> world`.
  - `camera -> simulate -> stop` החזיר `Stage View camera -> camera -> camera`.
  - `web_game_playwright_client` רץ אחרי השינוי ללא שגיאת runtime חדשה.
- מסך הפתיחה עבר שדרוג מבני ועיצובי:
  - שם האפליקציה ו־`Project Launcher` הועברו לאזור hero עליון מלא מעל שתי העמודות, בהתאם ל־layout שסומן.
  - `ProjectLauncher` קיבל hero חדש עם branding בולט, כותרת גדולה, טקסט פתיחה קצר ו־`Session Flow` panel בצד.
  - העמודה השמאלית הפכה ל־`Workspace Notes` במקום להכפיל את ה־branding הישן.
  - `index.css` עודכן עם grid areas חדשים ל־launcher, hero gradient מדורג, variant חדש ל־brand, וכרטיסי session steps ממוספרים.
- אימות:
  - screenshot דסקטופ (`launcher-hero.png`) אישר שהכותרת הועברה לאזור העליון ושומרת על hierarchy נקי ומקצועי.
  - screenshot מובייל full-page (`launcher-hero-mobile-full.png`) אישר שה־launcher החדש נשבר נכון לעמודה אחת ושהכרטיסים עדיין נגישים.
  - נוסף `overflow: auto` ל־launcher shell/overlay ו־mobile alignment ל־top כדי למנוע חיתוך תוכן במסכים צרים.
  - `web_game_playwright_client` רץ אחרי השינוי ללא `errors-0.json` חדש.
  - `npm run lint` עדיין נכשל רק על שגיאות TypeScript ישנות ב־`App.tsx` ו־`project-utils.ts`, לא על שינוי ה־launcher.
- שונה flow הכניסה כך שה־`Project Launcher` נפתח כברירת מחדל כ־overlay מעל העורך הראשי, במקום להחליף את כל האפליקציה במסך פתיחה נפרד.
  - `App.tsx`: `sessionStarted` מאותחל עכשיו ל־`true`, הוסר early-return הישן של מסך פתיחה נפרד, וה־launcher ממשיך להירנדר דרך `launcherOpen` כ־overlay עם `onClose`.
  - התוצאה: בכניסה למנוע רואים את העורך מאחור עם blur/dim overlay, כפי שביקש המשתמש.
- נוספה תמיכה ברורה בשם פרויקט:
  - `ProjectLauncher` קיבל שדה `New Blank Project Name` לפני כרטיסי ה־create/open.
  - `New Blank Project` משתמש בשם שהוזן שם במקום תמיד `Untitled Project`.
  - נוסף section חדש `Project` ב־`Details` כאשר אין selection, עם `Project Name` ו־`Project Description`, כך שאפשר לשנות את שם הפרויקט גם מתוך המסך הראשי.
- אימות:
  - בדיקת Playwright אישרה שבכניסה `launcherOpen` נראה כ־overlay ושה־topbar של העורך נראה מאחור (`overlayVisible = true`, `topbarVisible = true`).
  - הזנת `Nebula Workshop` בשדה החדש ב־launcher יצרה פרויקט עם אותו שם, והשם הופיע גם ב־topbar וגם ב־`Details`.
  - שינוי `Project Name` בתוך `Details` ל־`Nebula Workshop Renamed` עדכן מיידית את הכותרת ב־topbar.
  - screenshots: `launcher-entry-overlay.png`, `project-name-rename.png` אישרו ויזואלית את שני המצבים.
  - `web_game_playwright_client` רץ אחרי השינוי ללא `errors-0.json` חדש.
- שורת המשנה של `New Blank Project` קוצרה שוב כדי להוריד גובה ולצמצם wrap בכרטיס ה־blank project.
- כפתור `Create` בכרטיס `New Blank Project` הושווה לגובה שדה שם הפרויקט.
  - `index.css`: בתוך `.nexus-launcher-blank-row` גם ה־input וגם ה־button מוגדרים עכשיו ל־`height: 30px`, והכפתור עבר ל־flex centering במקום גובה קשיח גדול יותר.
- אימות:
  - בדיקת Playwright מדדה `inputHeight = 30` ו־`buttonHeight = 30`.
  - screenshot (`launcher-create-height.png`) אישר ויזואלית שהכפתור והשדה על אותו קו ובאותו גובה.
- שודרג ה־left sidebar וה־World Outliner:
  - `index.css`: רוחב הסרגל השמאלי הוגדל ל־`296px`, זהה לסרגל הימני.
  - `App.tsx`: נוספו `WorldOutlinerMode`, state ל־`List/Tree`, ו־expand/collapse state לצמתי ה־outliner.
  - `World Outliner` קיבל toggle בראש הסקשן בין `List` ל־`Tree`.
  - ב־`Tree` מוצגות סצנות כצמתי־על, ו־actors מתחתיהן עם היררכיית `parent/children` אם קיימת, scene switching, entity selection, ו־delete מתוך העץ.
  - נוספו helperים ל־build/filter/render של עץ entities לפי scenes.
  - תוקנה גם רגרסיה זמנית: ה־icon `Map` דרס את הבנאי הגלובלי `Map`; ה־tree helpers משתמשים עכשיו ב־`globalThis.Map`.
- אימות:
  - בדיקת Playwright אישרה `leftWidth = 296` ו־`rightWidth = 296`.
  - בטעינת `Platformer Sample`, tree mode הציג `1` scene node ו־`9` actor nodes.
  - מעבר ל־`List` החזיר מצב ללא scene nodes (`treeSceneCount = 0`), כלומר ה־toggle עובד.
  - בדיקת multi-scene אחרי `Blank` נוסף הראתה `sceneNodes = 2`, כלומר ה־tree מייצג סצנות מרובות.
  - screenshot `outliner-tree-desktop.png` אישר ויזואלית את רוחב הפנל ואת מצב ה־tree.
  - בדיקת runtime ישירה אחרי התיקון החזירה רק הודעות Vite/Phaser רגילות, ללא `pageerror` חדש.
- שודרג ה-World Outliner לרמת `Scene > Object > Components` עם component nodes אמיתיים, expand/collapse תקין גם ל-entities, ולחיצה על component שממקדת את ה-Details לאותו רכיב.
- הוחלפו כפתורי `Remove` של רכיבי Inspector לאייקוני פח קומפקטיים (`RigidBody`, `Collider`, `Behavior`, `Script`).
- `Place Actors` הודק ויזואלית: tiles קטנים וצפופים יותר עם line clamp ל-hints וגובה אחיד של `60px`.
- תיקון אינטראקציה: expand של entity nodes השתמש קודם ב-default שגוי ולכן הקליק הראשון לא פתח כלום; כעת entity toggles משתמשים ב-default collapsed עקבי.
- אימות: `web_game_playwright_client` רץ אחרי השינוי ללא `errors-0.json` חדש.
- אימות DOM/ויזואלי נוסף עם Playwright: בעץ הוצגו `1` scene, `9` object rows, ו-`5` component rows על `Player`; ה-selection של `Collider` סונכרן ל-Details והצילום `outliner-components-full.png` אישר את ההיררכיה, אייקוני הפח והדחיסה של `Place Actors`.
- חודד workflow ה-Details מול ה-tree: בחירת scene או actor מה-World Outliner מנקה עכשיו את `detailsFilter`, כך שלחיצה על actor מציגה שוב את כל הקומפוננטות שלו ולא נשארת נעולה על component קודם.
- `Place Actors` הוקטן פעם נוספת: tiles ירדו ל-`48px` גובה עם `padding` ו-`gap` מצומצמים יותר כדי להוריד whitespace אנכי.
- אימות נוסף עם Playwright:
  - אחרי `Collider -> Player` ב-tree, ה-Details חזרו להציג `Entity`, `Transform`, `Sprite`, `RigidBody`, `Collider`, `Behavior`, וה-`rightFilterValue` חזר ל-`""`.
  - גובה כל tile ב-`Place Actors` נמדד כ-`48px`.
  - screenshots זמניים `tree-actor-details-reset.png` ו-`place-actors-compact.png` נבדקו ידנית ואישרו את ה-fix, ואז נוקו מ-`output/web-game`.
- `Place Actors` חודד שוב: כל tile משתמש עכשיו ב-actor icon משמאל, ושכבת הטקסט מוגבלת ל-`2` שורות סה"כ (`title` שורה אחת + `hint` שורה אחת עם ellipsis).
- נוספה state מפורשת ל-`focusedOutlinerComponentId`, כך שבחירת component מדגישה רק את ה-component row, ובחירת actor מחזירה הדגשה רק ל-actor row.
- נוסף resize handle לסרגל השמאלי, עם drag שמרחיב את רוחב ה-outliner בטווח `296px -> 520px`.
- אימות נוסף עם Playwright:
  - `Place Actors` הציג `8` icons ו-`hintHeights = 10px` לכל הכרטיסים, בלי wrap מעבר לשורה אחת.
  - במדידה הסופית אחרי הידוק ה-ellipsis: כל tile ב-`Place Actors` ירד ל-`36px`, עם `titleHeights = 11px` ו-`hintHeights = 10px`.
  - בזמן בחירת `Collider` ב-tree: `activeObjectLabels = []`, `activeComponentLabels = ['Collider']`.
  - בזמן חזרה ל-`Player`: `activeObjectLabels = ['Player']`, `activeComponentRows = 0`, וה-Details הציגו שוב את כל הסקשנים.
  - drag על resize handle הרחיב את הסרגל השמאלי מ-`296px` ל-`416px`.

- תוקנה שכבת ה-AI generation כדי למנוע JSON truncation עם Vertex AI:
  - הוסר esponseJsonSchema מהקריאה שמחזירה JSON, כי הוא גרם בפועל לפלט שבור עם Gemini.
  - הוחזר 	hinking בצורה בטוחה יותר: שלב planning קצר עם ThinkingLevel.HIGH, ואז שלב JSON נפרד ללא thinking כדי לא לבזבז את תקציב הפלט.
  - maxOutputTokens ל-JSON הוגדל ל-16384, ונוסף repair pass ל-JSON שבור.
  - 
ormalizeAiResponse הוקשח כך שיקבל גם וריאציות תקינות חלקית של summary/notes/project.
- אימות:
  - POST ישיר ל-/api/ai/generate-project עם createDefaultProject() והבקשה create the snake game for me החזיר 200 עם JSON תקין.
  - 
pm run lint עדיין נכשל, אבל על שגיאות TypeScript ישנות ב-App.tsx/project-utils.ts שאינן חלק מהתיקון.


- נוסף AI debug trace מלא ל-editor:
  - ה-server מחזיר עכשיו debug עם plan/raw generation/raw repair, סטטיסטיקות פרויקט, finish reasons ו-validation issues.
  - כל ריצת AI נשמרת לקובץ תחת output/ai-debug/*.json.
  - פאנל ה-Assistant מציג חלון debug רציני עם warnings, raw responses, ו-Normalized Project JSON, כולל Copy/Download trace.
  - אחרי AI run נשארים בטאב Assistant כדי לראות מיד את ה-trace ולא נזרקים ל-Inspector.
- אימות:
  - קריאת create the snake game for me החזירה 200 עם debug.savedFilePath תקין.
  - במקרה הבדיקה: outputEntityCount=6, scriptCount=2, ehaviorCount=0, וה-validator סימן mpty-script פעמיים.
  - Playwright client רץ בלי rrors-0.json חדש; screenshot headless נשאר שחור כבעיית capture מוכרת של WebGL headless, אבל state file נוצר.

- חיזוק נוסף ל-prompt contract: Script components חייבים להכיל קוד לא ריק, ו-create mode חייב להחזיר game loop runnable ולא רק layout.
- ניסיון ריצה נוסף אחרי חיזוק ה-contract נתקע על latency/timeout של המודל, אבל /api/health נשאר תקין; כלומר אין crash בשרת.

- נוסף ערך cameraSize ל-scene settings והוצגו שדות Camera Width / Camera Height ב-Scene Settings, בלי לשנות את מבנה ה-editor.
- תצוגת ה-stage הוחזרה להתנהגות הקודמת: ה-canvas שוב ממלא את אזור העבודה, וגודל המצלמה משפיע רק על frame המצלמה ועל camera/play zoom במנוע.
- נוסף favicon סטטי דרך public/favicon.ico + public/favicon.svg וקישור ב-index.html כדי לבטל את שגיאת /favicon.ico 404.
- אימות:
  - בדיקת headless מאותו origin אחרי Create החזירה shellWidth=992, frameWidth=978, leftGap=7, rightGap=7; כלומר נשאר רק padding/מסגרת רגילים ולא הרווחים הגדולים שנוצרו קודם.
  - אותה בדיקה אישרה cameraWidthFieldVisible=true ו-faviconStatus=200.
- תוקן race ב-UI של selection gizmo בזמן מעבר World/Camera: ה-engine מעדכן את viewport mode ב-useLayoutEffect ונכפה rerender אחד נוסף של שכבת ה-gizmo, כדי שהידיות לא יישארו במיקום הישן עד hover.
- אימות ממוקד עם Playwright על הרצף Create -> Player -> Camera:
  - rotate handle לפני mousemove: x=794, y=370.703125
  - rotate handle אחרי mousemove: x=794, y=370.703125
  - כלומר dx=0, dy=0; אין יותר קפיצה/יישור מאוחר.
- screenshot ממוקד הראה את ה-Player במרכז עם gizmo מיושר מיד ב-camera view.
- web_game_playwright_client רץ שוב ללא errors-0.json; screenshot ה-headless השגרתי נשאר שחור בגלל capture של WebGL headless, ו-state-0.json היה mode=loading בלבד.

- 2026-03-15 18:24
  - תוקן desync של transform gizmo אחרי editor zoom/pan/resize.
  - src/App.tsx קיבל efreshViewportOverlay() שמכריח rerender מיידי של שכבת ה-HTML של ה-gizmo אחרי djustEditorZoom, panEditor, ngine.resize ומעבר World/Camera.
  - אימות Playwright ממוקד על Create -> Player -> zoom -> mousemove הראה dx=0, dy=0 לידית scale-nw, כלומר ה-gizmo לא זז יותר אחרי hover נוסף.


- 2026-03-15 18:56
  - מצב camera הודק למסגרת המצלמה עצמה: zoom-out כבר לא יכול לרדת מעבר לגודל המצלמה, ו-pan במצב camera לא יכול לצאת מחוץ ל-frame.
  - Core.ts משתמש עכשיו ב-camera viewport letterboxed, מחשב screen/world עם offset של ה-viewport, ומבצע clamp ייעודי ל-previewFrame במצב camera.
  - נוסף border בהיר למסגרת המצלמה במצב camera, וה-grid מוסתר בזמן play כדי שתצוגת הסימולציה תישאר תצוגת משחק ולא תצוגת editor.
  - אימות Playwright + ניתוח תמונה: ה-player נשאר באותו bbox בדיוק לפני ואחרי wheel zoom-out ולפני ואחרי pan (64x85, center 587.5,372.0).


- 2026-03-15 19:07
  - תוקן artifact של עובי שונה למסגרת במצב camera: גבול העולם כבר לא מצויר מתחת למסגרת המצלמה במצב זה, וה-frame של המצלמה נמשך עם inset זהה למסגרת העולם.
  - בוצע ב-Core.ts דרך suppression של world bounds border ב-drawSceneBackground() עבור ditorViewportMode === 'camera', וב-drawOverlay() frame המצלמה עובר ל-x+1/y+1 עם width-2/height-2.

- 2026-03-17
  - תוקן normalize של פלט AI "legacy/generic" שלא תאם בדיוק את סכמת Nexus ולכן נשבר ב-editor/runtime.
  - `src/lib/project-utils.ts` ממפה עכשיו גם:
    - `Behavior.behaviorType -> kind`
    - `RigidBody.bodyType -> isStatic` (וגם `gravityScale=0` ל-static אם לא סופק)
    - `Collider.shape: "rectangle" -> "box"`
    - `Collider.size.{x,y} -> width/height`
    - `Transform.scale` שמשמש בטעות כגודל authored ל-`Sprite.width/height`, ואז scale מתאפס ל-`1,1`
  - normalization בוחר fallback components לפי `type` במקום רק לפי index, ומשתמש ב-prefab של ה-entity כבסיס כשאין fallback קיים. זה מקטין אובדן מידע כשסדר הקומפוננטות משתנה.
  - `src/lib/ai-contract.ts` וה-guidance של `buildAiPromptContext()` חוזקו כדי להבהיר למודל:
    - `Transform.scale` הוא multiplier ולא שדה size.
    - להשתמש ב-`kind`, `isStatic`, ו-`box/circle` במקום `behaviorType`, `bodyType`, `rectangle`.
  - אימות ממוקד על ה-trace `output/ai-debug/2026-03-16T23-11-14-018Z-ai-mmtsryaq-np8y26.json`:
    - אחרי normalize: `behaviorCount=6`, הפלטפורמות static, ואין entities עם scale ענק.
    - import של הפרויקט המנורמל דרך ה-UI טען scene תקין (`Level 1`, `2000x800`) עם widths/heights שפויים לשחקן/פלטפורמות/collectibles.
    - `Simulate` + input קצר ב-Playwright החזירו `runtimeMode=play`, `runtimePlayer` בתזוזה, וללא console/page errors.
  - תוקן גם ה-resume/localStorage race:
    - `src/App.tsx`: autosave ל-`STORAGE_KEY` לא רץ יותר כשה-launcher עדיין פתוח, ולכן blank project לא דורס session שממתין ל-Resume.
    - אימות Playwright: `setItem -> reload` שומר עכשיו על `Mini Platformer with Boss`, ו-`Resume` טוען scene עם `10` entities וללא errors.

